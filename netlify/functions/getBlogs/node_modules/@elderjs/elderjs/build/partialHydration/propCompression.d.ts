export declare const isPrimitive: (thing: any) => boolean;
export declare const getType: (thing: any) => any;
export declare const walkAndCount: (thing: any, counts: Map<string, number>) => Map<string, number>;
export declare const getName: (num: number, counts: Map<string, number>, replacementChars: string) => string;
interface IPrepareSubsitutions {
    counts: Map<string, number>;
    substitutions: Map<string, string>;
    initialValues: Map<string, string>;
    replacementChars: string;
}
export declare const prepareSubstitutions: ({ counts, substitutions, initialValues, replacementChars, }: IPrepareSubsitutions) => void;
export declare const walkAndSubstitute: (thing: any, substitutions: Map<string, string>) => any;
export {};
