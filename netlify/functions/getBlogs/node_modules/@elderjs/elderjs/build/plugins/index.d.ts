import { ShortcodeDefs } from '../shortcodes/types';
import { HookOptions } from '..';
import { Elder } from '../Elder';
import { RoutesOptions } from '../routes/types';
export declare const pluginVersionCheck: (elderVersion: string, pluginVersion: string) => boolean;
declare function plugins(elder: Elder): Promise<{
    pluginRoutes: RoutesOptions;
    pluginHooks: HookOptions[];
    pluginShortcodes: ShortcodeDefs;
}>;
export default plugins;
