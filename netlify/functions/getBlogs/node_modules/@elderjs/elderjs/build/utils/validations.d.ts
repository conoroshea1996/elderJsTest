import type { SettingsOptions, PluginOptions, RollupSettings } from './types';
import type { ShortcodeDef } from '../shortcodes/types';
import type { RouteOptions } from '../routes/types';
import type { HookOptions } from '../hooks/types';
/**
 * Important note:
 * These validations are used to build the docs for Elder.js.
 */
declare const shortcodeSchema: any;
declare const configSchema: any;
/**
 * Defaults are set when routes are imported in route.ts
 */
declare const routeSchema: any;
declare const pluginSchema: any;
declare const hookSchema: any;
declare function getDefaultRollup(): RollupSettings;
declare function getDefaultConfig(): SettingsOptions;
declare function validateRoute(route: any, routeName: string): RouteOptions | false;
declare function validatePlugin(plugin: any): PluginOptions | false;
declare function validateHook(hook: any): HookOptions | false;
declare function validateShortcode(shortcode: any): ShortcodeDef | false;
export { validateRoute, validatePlugin, validateHook, validateShortcode, getDefaultRollup, getDefaultConfig, configSchema, hookSchema, routeSchema, pluginSchema, shortcodeSchema, };
