import { ComponentPayload } from './types';
export declare const getComponentName: (str: any) => any;
declare const svelteComponent: (componentName: String, folder?: String) => ({ page, props, hydrateOptions }: ComponentPayload) => string;
export default svelteComponent;
