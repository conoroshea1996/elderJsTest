"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const normalizePrefix = (prefix) => {
    if (!prefix)
        return '';
    // remove trailing slash
    const trimmed = prefix.replace(/\/+$/, '');
    // add leading slash
    return trimmed[0] === '/' ? trimmed : `/${trimmed}`;
};
exports.default = normalizePrefix;
