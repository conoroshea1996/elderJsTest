"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable no-param-reassign */
const glob_1 = __importDefault(require("glob"));
const path_1 = __importDefault(require("path"));
const fs_extra_1 = __importDefault(require("fs-extra"));
function getPluginLocations(elderConfig) {
    const pluginNames = Object.keys(elderConfig.plugins);
    return pluginNames.reduce((out, pluginName) => {
        const pluginPath = path_1.default.resolve(elderConfig.srcDir, `./plugins/${pluginName}`);
        const nmPluginPath = path_1.default.resolve(elderConfig.rootDir, `./node_modules/${pluginName}`);
        if (fs_extra_1.default.existsSync(`${pluginPath}/index.js`)) {
            const svelteFiles = glob_1.default.sync(`${pluginPath}/*.svelte`);
            if (svelteFiles.length > 0) {
                out.paths.push(`${pluginPath}/`);
                out.files = out.files.concat(svelteFiles);
            }
        }
        else if (fs_extra_1.default.existsSync(`${nmPluginPath}/package.json`)) {
            const svelteFiles = glob_1.default.sync(`${nmPluginPath}/*.svelte`);
            if (svelteFiles.length > 0) {
                out.paths.push(`${nmPluginPath}/`);
                out.files = out.files.concat(svelteFiles);
            }
        }
        return out;
    }, { paths: [], files: [] });
}
exports.default = getPluginLocations;
