import { InitializationOptions } from '../utils/types';
export declare function getWorkerCounts(counts: any): {
    count: number;
    errors: number;
};
declare function build(initializationOptions?: InitializationOptions): Promise<void>;
export default build;
