import { PreprocessorGroup } from 'svelte/types/compiler/preprocess/types';
import { InitializationOptions, SettingsOptions } from '../utils/types';
export declare type TPreprocess = PreprocessorGroup | PreprocessorGroup[] | false;
export declare type TSvelteHandler = {
    config: SettingsOptions;
    preprocess: TPreprocess;
};
export declare function getSvelteConfig(elderConfig: SettingsOptions): TPreprocess;
export declare function getPackagesWithSvelte(pkg: any, elderConfig: SettingsOptions): any;
declare type TEsbuildBundler = {
    initializationOptions?: InitializationOptions;
    replacements?: {
        [key: string]: string | boolean;
    };
};
declare const esbuildBundler: ({ initializationOptions, replacements }?: TEsbuildBundler) => Promise<void>;
export default esbuildBundler;
