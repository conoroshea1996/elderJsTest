import { Plugin, PartialMessage } from 'esbuild';
import { PreprocessorGroup } from 'svelte/types/compiler/preprocess/types';
import { SettingsOptions } from '..';
export declare type cssCacheObj = {
    code: string;
    map: string;
    time: number;
    priority: number;
};
export declare type TCache = Map<string, {
    contents: string;
    css?: cssCacheObj;
    warnings: PartialMessage[];
    time: number;
    priority?: number;
    map?: string;
}>;
export declare type TPreprocess = PreprocessorGroup | PreprocessorGroup[] | false;
export interface IEsBuildPluginSvelte {
    type: 'ssr' | 'client';
    svelteConfig: any;
    elderConfig: SettingsOptions;
    sveltePackages: string[];
    startDevServer?: boolean;
}
declare function esbuildPluginSvelte({ type, svelteConfig, elderConfig, sveltePackages }: IEsBuildPluginSvelte): Plugin;
export default esbuildPluginSvelte;
